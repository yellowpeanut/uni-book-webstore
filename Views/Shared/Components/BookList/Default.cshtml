@model IEnumerable<Application.ViewModels.BookCardViewModel>

@{
    Layout = null;
}

@if(Model != null)
{
    <div class="mt-5">
        @foreach (var book in Model)
        {
            @await Component.InvokeAsync("BookCard", new {
                model = book,
                mode = ViewBag.Mode
            })
        }
    </div>
    @if (ViewBag.Mode == Application.Data.Enums.BookCardModes.Remove)
    {
        @if (Model.Any())
        {
            <div>
                <p class="text-primary">
                    Полная стоимость товаров: @Model.Sum(e => e.Price) рублей
                </p>
            </div>
        }
    }
}
